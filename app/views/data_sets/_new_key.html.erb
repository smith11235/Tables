<div id="create_key" >
	<%= form_for( [data_set, data_set.keys.build], :remote => true ) do |f| %>
		<div class="field">
			<%= f.label :name %><br />
			<%= f.text_field :name %>
		</div>
		<ul>
			<% data_set.fields.each do |field| %>
				<li>
				<%= check_box_tag 'field_ids[]', field.id -%>
				<%= field.name -%>
				</li>
			<% end %>
		</ul>

		<div class="actions">
			<%= f.submit( "Create Key" ) %>
		</div>
	<% end %>

	<script>
		$('#new_key').bind('ajax:success', function(event, xhr, status, data) {  
			$("#current_keys").prepend( data.responseText );
			resetJQueryUI('accordion','current_keys');
			$('#key_tabs').tabs('select',0);
			$("#current_keys").accordion('option','active',0);
		} );
	</script>
</div>
