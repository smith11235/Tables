data_set/X
	- show X
  - menu-jquery
	- show 'revision' created_at selector
		- sets the displayed data in DataTables
		
	- show keys:
	  - set on this data_set/X
		- set on revisions of data_set/X
	- create key:
	  - select 1 - X fields
	- propogate key:
		- save on all revisions
		
	- show 'changes' between revisions
		- requires key tracking
		- new/lost records
		- new/lost fields
		- changed fields

Key	name:string data_set:references
	has_many :key_fields
KeyField key:references field:references


api:
	- for logging: 
	  - $table = Table.new( "stats" )
	  - $table = Table.new( "stats", "id=5" ) # with parameters
		- $table = Table.new( "stats", "id=5", "pro-football-reference.com/stats/5/" )
		- $table.log( :notice => "Crawler starting load", :job_id => "18383" )
		- my_hash.keys.each_with_index do |key,i|
				$table.log( :i => i, :key => key, :value => my_hash[key] )
			end

deployment:
	run your own database app
	load to managed app (for less technical people)
